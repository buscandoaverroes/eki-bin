[pytest]
# Only collect the host-side suite. (Without testpaths, pytest grabs
# micropython/led_test.py via the *_test.py pattern and fails on `import machine`.)
testpaths = tests
python_files = test_*.py
